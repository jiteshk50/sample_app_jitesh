<%= link_to("<< Back to List", {:action => 'index'}) %>

<div>
  <h2>Delete Student</h2>

  <%= form_for(:student, :url => {:action => 'destroy', :id => @student.id}) do |f| %>

    <p>Are you sure you want to permanently delete this student?</p>

    <p ><%= @student.name %></p>

    <div>
      <%= submit_tag("Delete Student", :class => "btn btn-danger") %>
    </div>

  <% end %>
</div>
