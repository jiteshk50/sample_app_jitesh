<%= link_to("<< Back to List", {:action => 'index'}) %>

<div>
  <h2>Create Student</h2>

  <%= form_for(:student, :url => {:action => 'create'}, html: { multipart: true }) do |f| %>

   <%= render(:partial => "form" , :locals => {:f => f}) %>

    <div >
      <%= submit_tag("Create Student", :class => "btn btn-success") %>
    </div>

  <% end %>
</div>
