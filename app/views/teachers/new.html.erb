<%= link_to("<< Back to List", {:action => 'index'}) %>

<div>
  <h2>Create Teacher</h2>

  <%= form_for(:teacher, :url => {:action => 'create'}) do |f| %>

   <%= render(:partial => "form" , :locals => {:f => f}) %>

    <div >
      <%= submit_tag("Create Teacher", :class => "btn btn-success") %>
    </div>

  <% end %>
</div>
