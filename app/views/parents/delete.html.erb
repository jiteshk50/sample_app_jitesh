<%= link_to("<< Back to List", {:action => 'index'}) %>

<div>
  <h2>Delete Parent</h2>

  <%= form_for(:parent, :url => {:action => 'destroy', :id => @parent.id}) do |f| %>

    <p>Are you sure you want to permanently delete this parent?</p>

    <p ><%= @parent.parent_name %></p>

    <div>
      <%= submit_tag("Delete Parent", :class => "btn btn-danger") %>
    </div>

  <% end %>
</div>
